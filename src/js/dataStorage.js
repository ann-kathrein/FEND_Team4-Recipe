'use strict';

/*const recipe = document.querySelectorAll('.containerRecipe'); // div Recipe
const favouriteBtn = document.querySelectorAll('.favourite'); // SVG Heart

favouriteBtn.addEvenetListener('click', storeRecipe);

function storeRecipe() {
  event.preventDefault();
  localStorage.setItem('recipe', recipe);
  cloneRecipe(); // ?Function for cloning Recipe necessary?
}*/

